# Copie para .env e ajuste se quiser ativar autenticação.
# Sem essas variáveis o servidor fica aberto (uso local sem senha).

# PORT=3000

# Para proteger o admin com HTTP Basic Auth:
# ADMIN_USER=admin
# ADMIN_PASS=alguma-senha-forte
